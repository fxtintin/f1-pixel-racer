# F1 Pixel Racer - 维护工作流指南

## 📁 项目结构速查

```
F1Project/F1/
├── 📁 src/                    ← 源代码（经常修改）
│   ├── components/            ← UI组件
│   ├── pages/                 ← 页面
│   └── utils/                 ← 工具函数
│
├── 📁 teams/cars/             ← 车队图片（偶尔更新）
├── 📁 nation/flags/           ← 国旗图片（偶尔更新）
├── 📁 public/                 ← 静态资源（自动复制）
├── 📁 dist/                   ← 构建输出（自动生成，勿改）
│
├── 📝 package.json            ← 依赖配置
├── 📝 README.md               ← 项目说明
├── 📝 .gitignore              ← Git忽略规则
└── 🔧 各种脚本文件
```

---

## 🛠️ 维护途径对比

| 操作类型 | 推荐方式 | 适合场景 | 难度 |
|---------|---------|---------|------|
| **代码修改** | VS Code + 终端 | 功能开发、界面调整 | ⭐⭐ |
| **图片更新** | 文件管理器 | 更换车队/国旗图片 | ⭐ |
| **数据更新** | Python 脚本 | 获取最新F1数据 | ⭐ |
| **一键部署** | PowerShell 脚本 | 快速启动/发布 | ⭐ |

---

## 途径一：VS Code + 终端（推荐，专业开发）

### 1. 安装 VS Code

下载：https://code.visualstudio.com/

推荐插件：
- **ES7+ React/Redux/React-Native snippets** - React代码片段
- **Tailwind CSS IntelliSense** - CSS提示
- **GitLens** - Git增强
- **Prettier** - 代码格式化

### 2. 打开项目

```bash
# 在终端执行
code "Y:\软件\kimi\F1Project\F1"
```

### 3. 日常开发流程

```bash
# 1. 进入项目目录
cd "Y:\软件\kimi\F1Project\F1"

# 2. 安装依赖（首次或package.json修改后）
npm install

# 3. 启动开发服务器
npm run dev
# 浏览器自动打开 http://localhost:5173

# 4. 修改代码...
# 文件修改后浏览器会自动刷新

# 5. 构建生产版本
npm run build

# 6. 提交到GitHub
git add .
git commit -m "描述这次修改"
git push
```

---

## 途径二：文件管理器 + 脚本（简单维护）

适合：只更新图片、不修改代码

### 更新车队图片

```
1. 打开文件夹: Y:\软件\kimi\F1Project\F1\teams\cars\
2. 替换或添加图片（保持命名格式）
3. 双击运行: update-resources.bat
4. 完成！
```

### 更新国旗图片

```
1. 打开文件夹: Y:\软件\kimi\F1Project\F1\nation\flags\
2. 替换或添加图片
3. 双击运行: update-resources.bat
4. 完成！
```

---

## 途径三：GitHub Codespaces（在线开发，无需本地环境）

适合：没有 Node.js 环境，或想在手机/平板上修改

### 使用方法

1. 访问 https://github.com/fxtintin/f1-pixel-racer
2. 点击绿色按钮 **「<> Code」**
3. 选择 **「Codespaces」** 标签
4. 点击 **「Create codespace on main」**

### 在线开发优势

- ✅ 无需安装 Node.js
- ✅ 随时随地访问（有浏览器就行）
- ✅ 自动保存到 GitHub
- ✅ 免费额度足够个人使用

---

## 📊 数据更新流程

### 获取最新 F1 数据

```bash
# 方法1: 使用Python脚本（推荐）
cd "Y:\软件\kimi\F1Project\F1"
python scripts\fetch_f1_data.py

# 方法2: 手动下载（如果API不可用）
# 访问 https://api.jolpi.ca/ergast/f1/2025.json
# 保存到 data/2025/races.json
```

### 添加新的赛季数据

1. **修改年份配置** (`src/hooks/useF1Data.ts`):
```typescript
export const GROUND_EFFECT_YEARS = ['2022', '2023', '2024', '2025', '2026']; // 添加2026
```

2. **添加新车队图片**:
   - 复制图片到 `teams/cars/`
   - 命名格式: `车队名-2026.png`

3. **更新车队配置** (`src/utils/teams.ts`):
```typescript
{
  id: 'red_bull',
  name: 'Red Bull Racing',
  years: {
    '2022': { drivers: ['max_verstappen', 'perez'], carImage: '/teams/cars/redbull-2022.png' },
    // ... 其他年份
    '2026': { drivers: ['max_verstappen', 'lawson'], carImage: '/teams/cars/redbull-2026.png' }, // 新增
  },
}
```

4. **重新构建**:
```bash
npm run build
```

---

## 🔄 版本控制工作流

### 标准 Git 工作流

```bash
# 1. 查看当前状态
git status

# 2. 拉取最新代码（如果在其他设备修改过）
git pull

# 3. 修改文件...

# 4. 添加更改
git add .
# 或只添加特定文件: git add src/components/new-file.tsx

# 5. 提交更改
git commit -m "添加新功能: 描述修改内容"

# 6. 推送到 GitHub
git push
```

### 提交信息规范

| 类型 | 示例 |
|------|------|
| 功能添加 | `git commit -m "feat: 添加2026赛季支持"` |
| 错误修复 | `git commit -m "fix: 修复国旗显示问题"` |
| 图片更新 | `git commit -m "assets: 更新红牛车队2026款赛车图"` |
| 文档更新 | `git commit -m "docs: 更新README说明"` |

---

## 📦 备份策略

### 自动备份（GitHub）

每次 `git push` 都会自动备份到 GitHub，这是最安全的。

### 本地备份

```powershell
# 创建本地备份压缩包
$date = Get-Date -Format "yyyyMMdd"
Compress-Archive -Path "Y:\软件\kimi\F1Project\F1\*" -DestinationPath "Y:\备份\F1-Pixel-Racer-$date.zip" -Exclude @("node_modules", "dist")
```

### 需要手动备份的文件

如果不用 Git，至少备份这些：

```
必须备份:
├── src/                    ← 源代码
├── teams/cars/             ← 车队图片
├── nation/flags/           ← 国旗图片
├── package.json            ← 依赖配置
└── README.md               ← 说明文档

无需备份（可重新生成）:
├── node_modules/           ← 运行 npm install 恢复
├── dist/                   ← 运行 npm run build 恢复
└── data/                   ← 运行 python scripts/fetch_f1_data.py 恢复
```

---

## 🚀 快速操作清单

### 场景 1: 修改界面样式

```bash
# 1. 启动开发服务器
npm run dev

# 2. 在 VS Code 中修改 src/components/ 下的文件

# 3. 浏览器自动刷新查看效果

# 4. 满意后构建
npm run build

# 5. 提交
git add .
git commit -m "style: 优化车队卡片样式"
git push
```

### 场景 2: 添加新车队图片

```bash
# 1. 复制图片到 teams/cars/
# 例如: teams/cars/audi-2026.png

# 2. 更新 src/utils/teams.ts 配置

# 3. 构建
npm run build

# 4. 提交
git add .
git commit -m "assets: 添加奥迪车队2026赛季赛车图"
git push
```

### 场景 3: 更新 F1 数据

```bash
# 1. 获取最新数据
python scripts/fetch_f1_data.py

# 2. 提交数据变更
git add data/
git commit -m "data: 更新2025赛季最新数据"
git push
```

### 场景 4: 添加新功能

```bash
# 1. 创建新分支（可选，推荐）
git checkout -b feature/new-feature

# 2. 开发功能...

# 3. 测试
npm run dev

# 4. 合并回主分支
git checkout master
git merge feature/new-feature

# 5. 推送
git push
```

---

## 📝 维护日程建议

| 频率 | 任务 |
|------|------|
| 每次比赛后 | 运行 `fetch_f1_data.py` 更新数据 |
| 赛季开始前 | 添加新车队、新车手、新赛道 |
| 每月 | `git push` 确保代码备份到 GitHub |
| 发现 Bug 时 | 立即修复并提交 |

---

## ❓ 常见问题

**Q: 可以在多台电脑上开发吗？**
A: 可以！在新电脑上：
```bash
git clone https://github.com/fxtintin/f1-pixel-racer.git
cd f1-pixel-racer
npm install
npm run dev
```

**Q: 修改后网页没有变化？**
A: 检查：
1. 开发服务器是否运行（`npm run dev`）
2. 浏览器是否缓存（按 Ctrl+F5 强制刷新）
3. 修改的文件是否保存

**Q: 如何回滚到之前的版本？**
A: 
```bash
# 查看历史
git log --oneline

# 回滚到某次提交
git reset --hard 提交ID
```

**Q: 忘记提交，电脑坏了怎么办？**
A: 如果用了 Git 且推送到 GitHub，代码是安全的。只需在新电脑上 `git clone` 即可恢复。

---

## 🎯 推荐的工作流（总结）

**日常开发（有VS Code）:**
```
VS Code 编辑 → 终端运行 npm run dev → 浏览器预览 → git push
```

**简单维护（只改图片）:**
```
文件管理器替换图片 → 双击 update-resources.bat → 完成
```

**紧急修复（只有手机）:**
```
GitHub Codespaces 在线编辑 → 自动部署
```
