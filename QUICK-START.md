# F1 Pixel Racer - 快速操作指南

## 🎯 我该用什么工具？

```
┌─────────────────────────────────────────────────────────────┐
│  想做什么？                    │  用什么？                    │
├─────────────────────────────────────────────────────────────┤
│  修改网页界面/功能             │  VS Code + 终端              │
│  更换车队/国旗图片             │  直接替换 + update-resources │
│  获取最新F1比赛数据            │  Python 脚本                 │
│  手机访问网站                  │  start-safe-server.ps1       │
│  备份到GitHub                  │  git push                    │
│  紧急修改没有电脑              │  GitHub Codespaces           │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 常用命令速查

### 开发命令

```bash
# 启动开发服务器（自动刷新）
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

### Git 命令

```bash
# 查看状态
git status

# 添加所有更改
git add .

# 提交更改
git commit -m "描述修改"

# 推送到GitHub
git push

# 拉取最新代码
git pull
```

### Python 脚本

```bash
# 获取F1数据
python scripts/fetch_f1_data.py

# 检查缺失图片
python scripts/check_images.py
```

---

## 📂 重要文件位置

| 类型 | 文件夹 | 示例 |
|------|--------|------|
| 源代码 | `src/` | `src/components/RaceCard.tsx` |
| 车队图片 | `teams/cars/` | `teams/cars/redbull-2024.png` |
| 国旗图片 | `nation/flags/` | `nation/flags/中国-CN.png` |
| 样式配置 | `src/index.css` | 修改颜色和字体 |
| 颜色主题 | `tailwind.config.js` | 修改F1红等颜色 |

---

## 🚀 场景速查

### 场景 1：我想改一下界面颜色

```
1. 打开 VS Code
2. 打开 tailwind.config.js
3. 修改颜色值
4. 保存（浏览器自动刷新）
5. npm run build
6. git add . && git commit -m "style: 修改主题色" && git push
```

### 场景 2：我想添加2026赛季

```
1. 添加车队图片到 teams/cars/
2. 修改 src/utils/teams.ts 添加2026配置
3. 修改 src/hooks/useF1Data.ts 添加2026到年份列表
4. 运行 update-resources.bat
5. git add . && git commit -m "feat: 添加2026赛季" && git push
```

### 场景 3：比赛结束了，更新成绩数据

```
1. 运行 python scripts/fetch_f1_data.py
2. git add data/ && git commit -m "data: 更新最新比赛数据" && git push
```

### 场景 4：我想在手机上看效果

```
1. 运行 start-safe-server.ps1
2. 手机浏览器访问 http://192.168.2.215:8080
```

---

## ⚡ 最快上手路径

**如果你只想简单维护（不改代码）：**

```
双击运行：
├── update-resources.bat     ← 更新图片后运行
├── start-safe-server.ps1    ← 启动服务器给手机访问
└── test-lan-access.ps1      ← 测试网络连通性
```

**如果你想深度定制：**

```
1. 安装 VS Code
2. 打开文件夹 Y:\软件\kimi\F1Project\F1
3. 按 Ctrl+` 打开终端
4. 运行 npm run dev
5. 开始修改代码...
```

---

## 📱 不同设备的操作方式

| 设备 | 推荐方式 |
|------|---------|
| 你的电脑（开发） | VS Code + 终端 |
| 你的电脑（快速维护） | 双击批处理脚本 |
| 其他电脑 | git clone + VS Code |
| 手机/平板 | GitHub Codespaces（浏览器） |
| 任何地方 | GitHub 网页直接编辑 |

---

## 🆘 遇到问题？

| 问题 | 解决方法 |
|------|---------|
| 忘记命令了 | 查看本文件或 WORKFLOW.md |
| 代码报错 | 运行 diagnose-network.ps1 |
| 手机连不上 | 查看 check-router-ap-isolation.md |
| 图片不显示 | 运行 update-resources.bat |
| 想恢复之前版本 | git log 查看历史，git reset 回滚 |

---

## 💡 最佳实践

1. **经常提交**：小修改就 `git commit`，不要攒一大堆
2. **写好说明**：提交信息写清楚做了什么
3. **先备份再改**：重要修改前先 `git push` 备份当前状态
4. **测试再推送**：本地测试没问题再 `git push`
5. **保持简洁**：只提交必要文件（node_modules 和 dist 已通过 .gitignore 忽略）
